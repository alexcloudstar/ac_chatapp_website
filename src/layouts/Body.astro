<>
  <slot />
</>

<style is:global>
  :root {
    --primary: #596787;
    --secondary: rgb(59 130 246 / 1);
  }

  .primary-font {
    font-family: 'Poppins', sans-serif;
    color: var(--primary);
  }

  .secondary-font {
    font-family: 'Roboto', sans-serif;
    color: var(--secondary);
  }
</style>
